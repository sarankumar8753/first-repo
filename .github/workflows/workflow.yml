name:  Learning GHA Concepts
on: [push]
jobs:
    practice:
        runs-on: macos-latest
        steps:
            - name: homebrew package update
              run: brew update -y
            - name: git installation
              run: brew install git -y

    duplicate:
        needs: practice
        runs-on: macos-latest
        steps:
            - name: homebrew package update
              run: brew update -y
            - name: tree installation
              run: brew install tree -y
              